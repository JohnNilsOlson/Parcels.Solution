@using Parcels.Models;

<h1>Your Packages</h1>
@if (Model.Count == 0)
{
  <p>There are no items in the list.</p>
}
  @foreach (Parcel newParcel in Model)
  { <ul>
      <li>@newParcel.Volume()</li>
      <li>@newParcel.CostToShip()</li>
    </ul>
  }